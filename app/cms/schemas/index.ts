import logoBgColorSchemas from "./logo-color-schema"
import pageSchemas from "./page-schema"
import { postSchemas } from "./post-schema"

export const schemaTypes = [...postSchemas, pageSchemas, logoBgColorSchemas]
